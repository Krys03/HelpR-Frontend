import React from 'react';

const NotFound = (props) => {
	return(
		<div>
			
				<img src={require('../../media/error2.jpg')}  alt="Page not found" style={{height: '100vh',
display: 'flex',
flexDirection: 'column',
alignItems: 'center',
justifyContent: 'center'}}/>
			</div>
		
		)
}

export default NotFound